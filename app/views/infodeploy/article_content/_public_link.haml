- @catenate=Infodeploy::Catenate.where(:article_id => @article.id).first
%p
  %label 标题:
  = @catenate.title
%p
  %label 链接:
  = @catenate.path
%p  
  %label 是否启用:
  - if @catenate.enabled == "1"
    启用
  - else
    禁用
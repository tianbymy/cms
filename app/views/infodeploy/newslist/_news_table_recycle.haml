%section
  %table.list
    %thead
      %tr
        = render "infodeploy/newslist/article_title"
        %th 操作
        %th 是否发布
    %tbody
      - newses.each_with_index do |news,count|
        %tr
          = render "infodeploy/article_table_content/#{news.type}",:count => count,:news => news
          %td
            = link_to "预览", article_waiting_audit_show_path(:article_id => news.id)
            |
            = link_to "编辑",edit_article_path(:id => news.id)
          %td
            = link_to "恢复",recover_recycles_path(:article_id => news.id),:method => :post, confirm: "您是否要恢复此文章?"
            |
            = link_to "删除",recycle_path(:id => news.id),:method => :delete, confirm: "您是否要删除所选文章?文章删除将不能恢复!"

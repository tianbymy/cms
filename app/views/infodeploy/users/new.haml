%header
  %h2 分配其它用户角色
  %p{:style => "color: green "}= flash[:notice]

  = form_for(@user) do |f|
    = render 'form', :f => f
    .action
      = f.submit "新建"

  = link_to "返回", users_path
  = link_to "更新用户数据", sync_users_path